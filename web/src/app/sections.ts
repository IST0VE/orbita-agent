/**
 * Разделы приложения и виды рабочей области.
 *
 * Два разных понятия, которые раньше стояли в одном ряду шапки и спорили
 * друг с другом: «Аналитика» выглядела разделом продукта, хотя была видом
 * рабочей области, а сценарий выглядел настройкой, хотя определял всю работу.
 *
 * Раздел — это где я нахожусь: рабочая область, настройки и журнал ошибок. Вид — это на что
 * я смотрю внутри рабочей области: схема, результат, документ. Сценарий —
 * это чем я работаю, и он не раздел вовсе.
 *
 * Разделом была и «База знаний» — список опубликованных документов. Она
 * ушла: список тот же самый ресурс, что и секция в колонке результатов, а
 * читались документы всё равно в главной области, куда их открывает
 * `publication.open`. Раздел добавлял к колонке одно поле фильтра по имени —
 * оно переехало в саму секцию.
 */

export type AppSection = "workspace" | "settings" | "journal";

export type WorkspaceView = "graph" | "result" | "document";

export type SettingsGroupId =
  | "ai"
  | "connections"
  | "integrations"
  | "execution"
  | "appearance"
  | "advanced";
