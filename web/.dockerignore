# Контекст сборки веб-образа: только то, что нужно `npm ci` и `vite build`.
node_modules/
dist/
.vite/
.tmp/
tests/
*.tsbuildinfo

# Личный web/.env с VITE_API_URL для dev-сервера не должен попасть в сборку:
# адрес встроился бы в код, и собранный фронт ходил бы мимо своего прокси.
.env
.env.*
